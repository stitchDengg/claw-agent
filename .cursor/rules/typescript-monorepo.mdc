---
description: TypeScript Monorepo 编码规范和架构纪律
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript Monorepo 规范

## 类型安全

- 禁止使用 `any`，除非绝对必要并附带注释说明原因
- 禁止 `as any`、无理由的 `@ts-ignore` / `@ts-expect-error`
- 优先使用高级类型推断（`infer`、泛型约束、条件类型、映射类型）
- 外部数据边界（API 响应、用户输入）必须有运行时 schema 验证（Zod 等）

## React 性能

- `useEffect` 依赖数组必须完整且最小化，需要清理的必须提供 cleanup 函数
- `useMemo` / `useCallback` 需要有正当理由，依赖数组必须正确
- 避免 `useContext` 导致大范围子树不必要的重渲染
- 状态管理订阅粒度要细：只订阅需要的字段，不订阅整个 store slice

## 异步流健壮性

- WebSocket / SSE / EventSource: 必须处理连接错误、实现退避重连、组件卸载时完整清理
- Fetch 链: 使用 AbortController，信号必须传递
- 防止竞态条件：处理组件卸载后的异步回调、过期闭包、乱序响应
- 使用 Error Boundaries 隔离 UI 故障

## Monorepo 架构纪律

- 跨包导入必须使用 workspace 协议（如 `workspace:*`）
- 绝对禁止包间循环依赖
- 不可导入其他包的内部（未导出）模块
- 遵循现有代码风格、命名约定和文件组织模式

## 实现前必做

1. 定位目标包，了解其职责
2. 搜索可复用的组件、hooks、工具函数、类型定义
3. 了解现有导入模式和包别名
4. 类型定义先行，再实现逻辑
